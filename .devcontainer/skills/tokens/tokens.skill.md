# Tokens — récap consommation

Placeholder. Le vrai récap CLI arrive en session 2 (`recap.js`).

Pour l'instant, le skill `tokens` ne fait qu'une chose : capturer les tokens
consommés à chaque Stop hook, dans un JSONL par session sous
`<project-root>/.claude/tokens/logs/YYYY-MM/<session_id>.jsonl`.

Voir [ROLLOUT.md](/workspace/plans/tokens/ROLLOUT.md) pour la roadmap.
