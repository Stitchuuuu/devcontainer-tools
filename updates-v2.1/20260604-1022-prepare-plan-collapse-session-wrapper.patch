diff --git a/.devcontainer/skills/prepare-plan/prepare-plan.skill.md b/.devcontainer/skills/prepare-plan/prepare-plan.skill.md
--- a/.devcontainer/skills/prepare-plan/prepare-plan.skill.md
+++ b/.devcontainer/skills/prepare-plan/prepare-plan.skill.md
@@ -77,10 +77,9 @@
 - **New single session** → proceed to step 1 with `mode = single`.
 - **Multi-session** → proceed to step 1 with `mode = multi`.
 
-`single` and `multi` produce **byte-identical scaffolds** apart from one line
-in `sessions/session-1-*.md` (the "Next session" block, substituted via
-`{{mode_next_session_hint}}`). The mode is purely a framing signal to the
-implementation session about whether to expect follow-up.
+`single` and `multi` produce **identical scaffolds**. The mode is purely a
+framing signal to the user about whether to expect follow-up — the session
+prompt's DoD already covers both outcomes inline.
 
 Never call `AskUserQuestion` a second time in the same invocation.
 
@@ -148,12 +147,6 @@
 | `{{date}}` | today ISO |
 | `{{first_session_slug}}` | first session slug |
 | `{{existing_body}}` | exploration findings, or fill-me stub |
-| `{{mode_next_session_hint}}` | see below — depends on chosen mode |
-
-`{{mode_next_session_hint}}` resolution:
-
-- `mode = single` → `None — single-session rollout, mark complete on STATUS.md after session 1 finishes.`
-- `mode = multi`  → `To be defined at end of session 1 — add a new row in STATUS.md and create sessions/session-2-<slug>.md following the same minimal shape as this file.`
 
 Write order:
 
@@ -190,7 +183,7 @@
 
 To run session 1 :
   1. Open sessions/session-1-{{first_session_slug}}.md
-  2. Copy the "Prompt to paste" block (between the 5-backtick fences)
+  2. Copy the file content (the file is the prompt)
   3. Paste into a new Claude Code session
   4. At the end, the session's DoD prescribes the STATUS / LOG updates
 
@@ -354,11 +347,6 @@
 ````markdown
 # Session 1 — {{first_session_slug}}
 
-> **Effort** : ~0.5–1 day | **Dependencies** : none (first session)
-
-## Prompt to paste
-
-`````
 I'm starting session 1 of the `{{feature_name}}` rollout.
 
 Entry point : `/workspace/plans/{{feature_name}}/ROLLOUT.md`
@@ -366,7 +354,6 @@
 - `STATUS.md` (where we are)
 - `LOG.md` (what's been done so far — empty on session 1)
 - `EXISTING.md` (current code inventory)
-- `sessions/session-1-{{first_session_slug}}.md` (this spec)
 
 Goal : {{description}}
 
@@ -385,17 +372,11 @@
    Commit.
 3. EXISTING.md : update if new files / structures were created.
 4. Propose a commit (do NOT commit without explicit user confirmation).
-`````
-
-## Next session
-
-{{mode_next_session_hint}}
 ````
 
-> 🚨 **The session template uses 5-backtick fences for the prompt block**
-> so the prompt can itself contain 4-backtick fences for embedded code
-> blocks if needed. When writing this file, keep the 5-backtick fences
-> intact.
+> The session file IS the prompt — no wrapper, no fence. The user copies the
+> file's full content into a fresh chat. Effort estimates, prerequisites, and
+> per-session metadata belong in STATUS.md (extra columns if needed), not here.
 
 ## Constraints
 
